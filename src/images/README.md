# Thư mục ảnh giao diện

- `icons/`: Chứa icon cho nút, menu, trạng thái.
- `backgrounds/`: Chứa ảnh nền cho màn hình đăng nhập/dashboard.
- `logos/`: Chứa logo khách sạn và logo hệ thống.

Gợi ý sử dụng trong Swing: nạp ảnh bằng `ImageIcon` và đặt vào `JLabel` hoặc `JButton`.
